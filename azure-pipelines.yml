trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
# --- STAGE 1: CONTINUOUS INTEGRATION (CI) ---
- stage: CI_Build
  displayName: 'CI: Build Application'
  jobs:
  - job: RunNpmBuild
    displayName: 'Compile Node Project'
    steps:
    - task: NodeTool@0
      inputs:
        versionSpec: '20.x'
      displayName: 'Install Node.js'

    # Injecting the environment variables directly into the compilation process
    - script: |
        npm install
        npm run build
      displayName: 'npm install and build'
      env:
        VITE_AUTH0_DOMAIN: 'dev-2gu3bz0k4hol5cg4.us.auth0.com'
        VITE_AUTH0_CLIENT_ID: 'cPAePPfmTjC43qgq9rVqsN7dOcKHELB1'
        VITE_AUTH0_AUDIENCE: 'mern-food-ordering-api'
        VITE_API_BASE_URL: 'https://merneats-backend-3nqo.onrender.com'

    # Inject Express into the build artifact directory for the server routing
    - script: |
        cp server.js dist/
        cd dist
        npm install express
      displayName: 'Prepare Production Web Server Environment'

    - task: PublishPipelineArtifact@1
      displayName: 'Save Built Files'
      inputs:
        targetPath: '$(System.DefaultWorkingDirectory)/dist'
        artifact: 'build_output'

# --- STAGE 2: CONTINUOUS DELIVERY (CD) ---
- stage: CD_Deploy
  displayName: 'CD: Deploy to Portal'
  dependsOn: CI_Build            
  condition: succeeded()          
  jobs:
  - job: ExecuteDeploy
    displayName: 'Push to Azure Web App'
    steps:
    - task: DownloadPipelineArtifact@2
      displayName: 'Retrieve Built Files'
      inputs:
        buildType: 'current'
        artifactName: 'build_output'
        targetPath: '$(Pipeline.Workspace)/drop'

    - task: AzureRmWebAppDeployment@4
      displayName: 'Deploy to Azure Web App'
      inputs:
        ConnectionType: 'AzureRM'
        azureSubscription: 'AzureRM'
        WebAppName: 'mycdpipe'
        Package: '$(Pipeline.Workspace)/drop'
